// Empty by default so a deploy that forgets to set this fails safe (records nothing) instead of
// silently running on sample data. Set to "mock" to run the sample-data backend in the browser
// (for trying the app locally). For the real app, paste the Apps Script web app URL here,
// e.g. "https://script.google.com/macros/s/AKfy.../exec".
export const API_URL = "";
